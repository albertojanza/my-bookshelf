<div class="book">
  <h1><%= @book.title %> </h1>
  <p class="authors"><%= " by #{ by_authors @book.author}"%></p>
  
  <%= link_to image_tag(@book.large_image,:width => 300), @book.detail_page_url %>
  <div class="info">
    <p> Friends that have read this book or are reading it </p>
    <% @friends_have_read_it.each do |user| %>
      <%= debug user %>
    <% end %>
asfasd
  </div>
  
  
  <div style="margin-top:50px;"
  <%= debug @book%>
  <h1> <%= "#{@book.title} by #{@book.author}" %> </h1>
  
  <div >
  
  <div class="left_float">
    <%= image_tag @book.medium_image%><br> 
  </div>
  <div class="left_float">
    <%= %>
  </div>
  
  </div>
</div>

