<% content_for :mainheader do %>

  <%= render 'submenu_my_bookshelf' %>
<div class="title">
<h1>REad books</h1>
</div>


<% end %>



<%= render 'slider',:title => title(3), :path => shelf_path(:id => @user, :code => 3), :partial => 'book_item', :collection => @recommended_books,  :items_slide => 3,:slider_index => 0 %>
<%= render 'slider',:title => title(2), :path => shelf_path(:id => @user, :code => 2), :partial => 'book_item', :collection => @next_books,  :items_slide => 3,:slider_index => 1 %>
<%= render 'slider',:title => title(1), :path => shelf_path(:id => @user, :code => 1), :partial => 'book_item', :collection => @reading, :items_slide => 3,:slider_index => 2  %>
<%= render 'slider',:title => title(0), :path => shelf_path(:id => @user, :code => 0), :partial => 'book_item', :collection => @read_books, :items_slide => 6,:slider_index => 3%>

<script type="text/javascript" >
    var sliders = [[0,<%= (@recommended_books.size + 2) / 3 %>],[0,<%= (@next_books.size + 2) / 3 %>],[0,<%= (@reading.size + 2) / 3 %>],[0,<%= (@read_books.size + 2) / 6 %>] ];
</script>    




<% content_for :sidebar do %>

  <div class="top_sidebar_shadow">
  </div>

  <div class="sidebar_shadow" style="clear:both;">
    <div style="padding:0 0px 5px 0;">
      <%= render 'widget_user' %>
    </div>
  </div>
  <div class="bottom_sidebar_shadow">
  </div>

<% end %>

