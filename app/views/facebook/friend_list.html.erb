<% content_for :mainheader do %>

  <%= render 'submenu_friends' %>
<% end %>


<div class="section-title">
    <h2> <%= I18n.t 'my_friends_in' %></h2>
</div>

<%= render :partial => 'friend', :collection => @friends_in %>
<div class="cleaner">
</div>
<div class="section-title">
    <h2> <%= I18n.t 'my_friends_out' %></h2>
</div>

<div>
<%= render :partial => 'friend_out', :collection => @friends_out %>

</div>

<% content_for :sidebar do %>

  <div class="top_sidebar_shadow">
  </div>

  <div id="sidebar_shadow" class="sidebar_shadow" style="clear:both;">
    <div id="sidebar_shadow_container" class="sidebar_shadow_container">
      <div id="book_similarities">
      </div>
    </div>
  </div>
  <div class="bottom_sidebar_shadow">
  </div>

<% end %>

          <% if @last_book = Book.first %>
              <script type="text/javascript" >
                   $("#book_similarities").load("<%= book_similarities_path(:asin => @last_book.asin) %>"); 
              </script>    
          <% end %>
