<div id="user-widget-<%= @user.id %>" class="user-widget">
    <div>
    <%= image_tag("http://graph.facebook.com/#{@user.uid}/picture",:width => 45  )%>
    <div class="user-name"><%= @user.name %></div>
    </div>
    <div class="cleaner"> </div>
    <div class="user-readings"><%= link_to "Rading #{@user.reading_count} books",shelf_path(:id => @user.id,:code => 1) %></div>
    <div class="user-readings"><%= link_to "Read #{@user.read_count} books",shelf_path(:id => @user.id,:code => 0) %></div>
</div>

